# Copyright (c) Ankit Chaubey <ankitchaubey.dev@gmail.com>
# SPDX-License-Identifier: MIT OR Apache-2.0
#
# ferogram is a high-performance Telegram MTProto framework written in Rust.
# ferogram-py provides Python bindings built on top of the Rust core for
# building Telegram clients, bots, and applications with a simple API.
#
# Rust core: https://github.com/ankit-chaubey/ferogram
# Python bindings: https://github.com/ankit-chaubey/ferogram-py
#
# If you use or modify this code, keep this notice at the top of the file
# and include the LICENSE-MIT or LICENSE-APACHE file from this repository.


# auto-generated by ferogram raw codegen - do not edit
from __future__ import annotations
from typing import Any
from ... import tl as _tl
from .._tl_schema import _SCHEMA


class ContactsNotModified:
    def __init__(self) -> None:
        pass

    def to_dict(self) -> dict:
        return {"_": "contacts.contactsNotModified", **{
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"ContactsNotModified()"

class Contacts:
    def __init__(
        self,
        contacts: list[Any],
        saved_count: int,
        users: list[Any],
    ) -> None:
        self.contacts = contacts
        self.saved_count = saved_count
        self.users = users

    def to_dict(self) -> dict:
        return {"_": "contacts.contacts", **{
            "contacts": _tl._resolve(self.contacts),
            "saved_count": _tl._resolve(self.saved_count),
            "users": _tl._resolve(self.users),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"Contacts(contacts={self.contacts!r}, saved_count={self.saved_count!r}, users={self.users!r})"

class ImportedContacts:
    def __init__(
        self,
        imported: list[Any],
        popular_invites: list[Any],
        retry_contacts: list[int],
        users: list[Any],
    ) -> None:
        self.imported = imported
        self.popular_invites = popular_invites
        self.retry_contacts = retry_contacts
        self.users = users

    def to_dict(self) -> dict:
        return {"_": "contacts.importedContacts", **{
            "imported": _tl._resolve(self.imported),
            "popular_invites": _tl._resolve(self.popular_invites),
            "retry_contacts": _tl._resolve(self.retry_contacts),
            "users": _tl._resolve(self.users),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"ImportedContacts(imported={self.imported!r}, popular_invites={self.popular_invites!r}, retry_contacts={self.retry_contacts!r}, users={self.users!r})"

class Blocked:
    def __init__(
        self,
        blocked: list[Any],
        chats: list[Any],
        users: list[Any],
    ) -> None:
        self.blocked = blocked
        self.chats = chats
        self.users = users

    def to_dict(self) -> dict:
        return {"_": "contacts.blocked", **{
            "blocked": _tl._resolve(self.blocked),
            "chats": _tl._resolve(self.chats),
            "users": _tl._resolve(self.users),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"Blocked(blocked={self.blocked!r}, chats={self.chats!r}, users={self.users!r})"

class BlockedSlice:
    def __init__(
        self,
        count: int,
        blocked: list[Any],
        chats: list[Any],
        users: list[Any],
    ) -> None:
        self.count = count
        self.blocked = blocked
        self.chats = chats
        self.users = users

    def to_dict(self) -> dict:
        return {"_": "contacts.blockedSlice", **{
            "count": _tl._resolve(self.count),
            "blocked": _tl._resolve(self.blocked),
            "chats": _tl._resolve(self.chats),
            "users": _tl._resolve(self.users),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"BlockedSlice(count={self.count!r}, blocked={self.blocked!r}, chats={self.chats!r}, users={self.users!r})"

class Found:
    def __init__(
        self,
        my_results: list[Any],
        results: list[Any],
        chats: list[Any],
        users: list[Any],
    ) -> None:
        self.my_results = my_results
        self.results = results
        self.chats = chats
        self.users = users

    def to_dict(self) -> dict:
        return {"_": "contacts.found", **{
            "my_results": _tl._resolve(self.my_results),
            "results": _tl._resolve(self.results),
            "chats": _tl._resolve(self.chats),
            "users": _tl._resolve(self.users),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"Found(my_results={self.my_results!r}, results={self.results!r}, chats={self.chats!r}, users={self.users!r})"

class ResolvedPeer:
    def __init__(
        self,
        peer: Any,
        chats: list[Any],
        users: list[Any],
    ) -> None:
        self.peer = peer
        self.chats = chats
        self.users = users

    def to_dict(self) -> dict:
        return {"_": "contacts.resolvedPeer", **{
            "peer": _tl._resolve(self.peer),
            "chats": _tl._resolve(self.chats),
            "users": _tl._resolve(self.users),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"ResolvedPeer(peer={self.peer!r}, chats={self.chats!r}, users={self.users!r})"

class TopPeersNotModified:
    def __init__(self) -> None:
        pass

    def to_dict(self) -> dict:
        return {"_": "contacts.topPeersNotModified", **{
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"TopPeersNotModified()"

class TopPeers:
    def __init__(
        self,
        categories: list[Any],
        chats: list[Any],
        users: list[Any],
    ) -> None:
        self.categories = categories
        self.chats = chats
        self.users = users

    def to_dict(self) -> dict:
        return {"_": "contacts.topPeers", **{
            "categories": _tl._resolve(self.categories),
            "chats": _tl._resolve(self.chats),
            "users": _tl._resolve(self.users),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"TopPeers(categories={self.categories!r}, chats={self.chats!r}, users={self.users!r})"

class TopPeersDisabled:
    def __init__(self) -> None:
        pass

    def to_dict(self) -> dict:
        return {"_": "contacts.topPeersDisabled", **{
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"TopPeersDisabled()"

class ContactBirthdays:
    def __init__(
        self,
        contacts: list[Any],
        users: list[Any],
    ) -> None:
        self.contacts = contacts
        self.users = users

    def to_dict(self) -> dict:
        return {"_": "contacts.contactBirthdays", **{
            "contacts": _tl._resolve(self.contacts),
            "users": _tl._resolve(self.users),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"ContactBirthdays(contacts={self.contacts!r}, users={self.users!r})"

class SponsoredPeersEmpty:
    def __init__(self) -> None:
        pass

    def to_dict(self) -> dict:
        return {"_": "contacts.sponsoredPeersEmpty", **{
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"SponsoredPeersEmpty()"

class SponsoredPeers:
    def __init__(
        self,
        peers: list[Any],
        chats: list[Any],
        users: list[Any],
    ) -> None:
        self.peers = peers
        self.chats = chats
        self.users = users

    def to_dict(self) -> dict:
        return {"_": "contacts.sponsoredPeers", **{
            "peers": _tl._resolve(self.peers),
            "chats": _tl._resolve(self.chats),
            "users": _tl._resolve(self.users),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"SponsoredPeers(peers={self.peers!r}, chats={self.chats!r}, users={self.users!r})"
