# auto-generated by ferogram raw codegen - do not edit

# Copyright (c) Ankit Chaubey <ankitchaubey.dev@gmail.com>
# SPDX-License-Identifier: MIT OR Apache-2.0
#
# ferogram is a high-performance Telegram MTProto framework written in Rust.
# ferogram-py provides Python bindings built on top of the Rust core for
# building Telegram clients, bots, and applications with a simple API.
#
# Rust core: https://github.com/ankit-chaubey/ferogram
# Python bindings: https://github.com/ankit-chaubey/ferogram-py
#
# If you use or modify this code, keep this notice at the top of the file
# and include the LICENSE-MIT or LICENSE-APACHE file from this repository.

from __future__ import annotations
from typing import Any
from ... import tl as _tl
from .._tl_schema import _SCHEMA


class AllStoriesNotModified:
    def __init__(
        self,
        state: str,
        stealth_mode: Any,
    ) -> None:
        self.state = state
        self.stealth_mode = stealth_mode

    def to_dict(self) -> dict:
        return {"_": "stories.allStoriesNotModified", **{
            "state": _tl._resolve(self.state),
            "stealth_mode": _tl._resolve(self.stealth_mode),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"AllStoriesNotModified(state={self.state!r}, stealth_mode={self.stealth_mode!r})"

class AllStories:
    def __init__(
        self,
        count: int,
        state: str,
        peer_stories: list[Any],
        chats: list[Any],
        users: list[Any],
        stealth_mode: Any,
        has_more: bool | None = None,
    ) -> None:
        self.has_more = has_more
        self.count = count
        self.state = state
        self.peer_stories = peer_stories
        self.chats = chats
        self.users = users
        self.stealth_mode = stealth_mode

    def to_dict(self) -> dict:
        return {"_": "stories.allStories", **{
            "has_more": _tl._resolve(self.has_more),
            "count": _tl._resolve(self.count),
            "state": _tl._resolve(self.state),
            "peer_stories": _tl._resolve(self.peer_stories),
            "chats": _tl._resolve(self.chats),
            "users": _tl._resolve(self.users),
            "stealth_mode": _tl._resolve(self.stealth_mode),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"AllStories(has_more={self.has_more!r}, count={self.count!r}, state={self.state!r}, peer_stories={self.peer_stories!r})"

class Stories:
    def __init__(
        self,
        count: int,
        stories: list[Any],
        chats: list[Any],
        users: list[Any],
        pinned_to_top: list[int] | None = None,
    ) -> None:
        self.count = count
        self.stories = stories
        self.pinned_to_top = pinned_to_top
        self.chats = chats
        self.users = users

    def to_dict(self) -> dict:
        return {"_": "stories.stories", **{
            "count": _tl._resolve(self.count),
            "stories": _tl._resolve(self.stories),
            "pinned_to_top": _tl._resolve(self.pinned_to_top),
            "chats": _tl._resolve(self.chats),
            "users": _tl._resolve(self.users),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"Stories(count={self.count!r}, stories={self.stories!r}, pinned_to_top={self.pinned_to_top!r}, chats={self.chats!r})"

class StoryViewsList:
    def __init__(
        self,
        count: int,
        views_count: int,
        forwards_count: int,
        reactions_count: int,
        views: list[Any],
        chats: list[Any],
        users: list[Any],
        next_offset: str | None = None,
    ) -> None:
        self.count = count
        self.views_count = views_count
        self.forwards_count = forwards_count
        self.reactions_count = reactions_count
        self.views = views
        self.chats = chats
        self.users = users
        self.next_offset = next_offset

    def to_dict(self) -> dict:
        return {"_": "stories.storyViewsList", **{
            "count": _tl._resolve(self.count),
            "views_count": _tl._resolve(self.views_count),
            "forwards_count": _tl._resolve(self.forwards_count),
            "reactions_count": _tl._resolve(self.reactions_count),
            "views": _tl._resolve(self.views),
            "chats": _tl._resolve(self.chats),
            "users": _tl._resolve(self.users),
            "next_offset": _tl._resolve(self.next_offset),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"StoryViewsList(count={self.count!r}, views_count={self.views_count!r}, forwards_count={self.forwards_count!r}, reactions_count={self.reactions_count!r})"

class StoryViews:
    def __init__(
        self,
        views: list[Any],
        users: list[Any],
    ) -> None:
        self.views = views
        self.users = users

    def to_dict(self) -> dict:
        return {"_": "stories.storyViews", **{
            "views": _tl._resolve(self.views),
            "users": _tl._resolve(self.users),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"StoryViews(views={self.views!r}, users={self.users!r})"

class PeerStories:
    def __init__(
        self,
        stories: Any,
        chats: list[Any],
        users: list[Any],
    ) -> None:
        self.stories = stories
        self.chats = chats
        self.users = users

    def to_dict(self) -> dict:
        return {"_": "stories.peerStories", **{
            "stories": _tl._resolve(self.stories),
            "chats": _tl._resolve(self.chats),
            "users": _tl._resolve(self.users),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"PeerStories(stories={self.stories!r}, chats={self.chats!r}, users={self.users!r})"

class StoryReactionsList:
    def __init__(
        self,
        count: int,
        reactions: list[Any],
        chats: list[Any],
        users: list[Any],
        next_offset: str | None = None,
    ) -> None:
        self.count = count
        self.reactions = reactions
        self.chats = chats
        self.users = users
        self.next_offset = next_offset

    def to_dict(self) -> dict:
        return {"_": "stories.storyReactionsList", **{
            "count": _tl._resolve(self.count),
            "reactions": _tl._resolve(self.reactions),
            "chats": _tl._resolve(self.chats),
            "users": _tl._resolve(self.users),
            "next_offset": _tl._resolve(self.next_offset),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"StoryReactionsList(count={self.count!r}, reactions={self.reactions!r}, chats={self.chats!r}, users={self.users!r})"

class FoundStories:
    def __init__(
        self,
        count: int,
        stories: list[Any],
        chats: list[Any],
        users: list[Any],
        next_offset: str | None = None,
    ) -> None:
        self.count = count
        self.stories = stories
        self.next_offset = next_offset
        self.chats = chats
        self.users = users

    def to_dict(self) -> dict:
        return {"_": "stories.foundStories", **{
            "count": _tl._resolve(self.count),
            "stories": _tl._resolve(self.stories),
            "next_offset": _tl._resolve(self.next_offset),
            "chats": _tl._resolve(self.chats),
            "users": _tl._resolve(self.users),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"FoundStories(count={self.count!r}, stories={self.stories!r}, next_offset={self.next_offset!r}, chats={self.chats!r})"

class CanSendStoryCount:
    def __init__(
        self,
        count_remains: int,
    ) -> None:
        self.count_remains = count_remains

    def to_dict(self) -> dict:
        return {"_": "stories.canSendStoryCount", **{
            "count_remains": _tl._resolve(self.count_remains),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"CanSendStoryCount(count_remains={self.count_remains!r})"

class AlbumsNotModified:
    def __init__(self) -> None:
        pass

    def to_dict(self) -> dict:
        return {"_": "stories.albumsNotModified", **{
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"AlbumsNotModified()"

class Albums:
    def __init__(
        self,
        hash: int,
        albums: list[Any],
    ) -> None:
        self.hash = hash
        self.albums = albums

    def to_dict(self) -> dict:
        return {"_": "stories.albums", **{
            "hash": _tl._resolve(self.hash),
            "albums": _tl._resolve(self.albums),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"Albums(hash={self.hash!r}, albums={self.albums!r})"
