# auto-generated by ferogram raw codegen - do not edit

# Copyright (c) Ankit Chaubey <ankitchaubey.dev@gmail.com>
# SPDX-License-Identifier: MIT OR Apache-2.0
#
# ferogram is a high-performance Telegram MTProto framework written in Rust.
# ferogram-py provides Python bindings built on top of the Rust core for
# building Telegram clients, bots, and applications with a simple API.
#
# Rust core: https://github.com/ankit-chaubey/ferogram
# Python bindings: https://github.com/ankit-chaubey/ferogram-py
#
# If you use or modify this code, keep this notice at the top of the file
# and include the LICENSE-MIT or LICENSE-APACHE file from this repository.

from __future__ import annotations
from typing import Any
from ... import tl as _tl
from .._tl_schema import _SCHEMA


class UpdateProfilePhoto:
    def __init__(
        self,
        id: Any,
        fallback: bool | None = None,
        bot: Any | None = None,
    ) -> None:
        self.fallback = fallback
        self.bot = bot
        self.id = id

    def to_dict(self) -> dict:
        return {"_": "photos.updateProfilePhoto", **{
            "fallback": _tl._resolve(self.fallback),
            "bot": _tl._resolve(self.bot),
            "id": _tl._resolve(self.id),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"UpdateProfilePhoto(fallback={self.fallback!r}, bot={self.bot!r}, id={self.id!r})"

class UploadProfilePhoto:
    def __init__(
        self,
        fallback: bool | None = None,
        bot: Any | None = None,
        file: Any | None = None,
        video: Any | None = None,
        video_start_ts: float | None = None,
        video_emoji_markup: Any | None = None,
    ) -> None:
        self.fallback = fallback
        self.bot = bot
        self.file = file
        self.video = video
        self.video_start_ts = video_start_ts
        self.video_emoji_markup = video_emoji_markup

    def to_dict(self) -> dict:
        return {"_": "photos.uploadProfilePhoto", **{
            "fallback": _tl._resolve(self.fallback),
            "bot": _tl._resolve(self.bot),
            "file": _tl._resolve(self.file),
            "video": _tl._resolve(self.video),
            "video_start_ts": _tl._resolve(self.video_start_ts),
            "video_emoji_markup": _tl._resolve(self.video_emoji_markup),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"UploadProfilePhoto(fallback={self.fallback!r}, bot={self.bot!r}, file={self.file!r}, video={self.video!r})"

class DeletePhotos:
    def __init__(
        self,
        id: list[Any],
    ) -> None:
        self.id = id

    def to_dict(self) -> dict:
        return {"_": "photos.deletePhotos", **{
            "id": _tl._resolve(self.id),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"DeletePhotos(id={self.id!r})"

class GetUserPhotos:
    def __init__(
        self,
        user_id: Any,
        offset: int,
        max_id: int,
        limit: int,
    ) -> None:
        self.user_id = user_id
        self.offset = offset
        self.max_id = max_id
        self.limit = limit

    def to_dict(self) -> dict:
        return {"_": "photos.getUserPhotos", **{
            "user_id": _tl._resolve(self.user_id),
            "offset": _tl._resolve(self.offset),
            "max_id": _tl._resolve(self.max_id),
            "limit": _tl._resolve(self.limit),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"GetUserPhotos(user_id={self.user_id!r}, offset={self.offset!r}, max_id={self.max_id!r}, limit={self.limit!r})"

class UploadContactProfilePhoto:
    def __init__(
        self,
        user_id: Any,
        suggest: bool | None = None,
        save: bool | None = None,
        file: Any | None = None,
        video: Any | None = None,
        video_start_ts: float | None = None,
        video_emoji_markup: Any | None = None,
    ) -> None:
        self.suggest = suggest
        self.save = save
        self.user_id = user_id
        self.file = file
        self.video = video
        self.video_start_ts = video_start_ts
        self.video_emoji_markup = video_emoji_markup

    def to_dict(self) -> dict:
        return {"_": "photos.uploadContactProfilePhoto", **{
            "suggest": _tl._resolve(self.suggest),
            "save": _tl._resolve(self.save),
            "user_id": _tl._resolve(self.user_id),
            "file": _tl._resolve(self.file),
            "video": _tl._resolve(self.video),
            "video_start_ts": _tl._resolve(self.video_start_ts),
            "video_emoji_markup": _tl._resolve(self.video_emoji_markup),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"UploadContactProfilePhoto(suggest={self.suggest!r}, save={self.save!r}, user_id={self.user_id!r}, file={self.file!r})"
