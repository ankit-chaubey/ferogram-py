# auto-generated by ferogram raw codegen - do not edit

# Copyright (c) Ankit Chaubey <ankitchaubey.dev@gmail.com>
# SPDX-License-Identifier: MIT OR Apache-2.0
#
# ferogram is a high-performance Telegram MTProto framework written in Rust.
# ferogram-py provides Python bindings built on top of the Rust core for
# building Telegram clients, bots, and applications with a simple API.
#
# Rust core: https://github.com/ankit-chaubey/ferogram
# Python bindings: https://github.com/ankit-chaubey/ferogram-py
#
# If you use or modify this code, keep this notice at the top of the file
# and include the LICENSE-MIT or LICENSE-APACHE file from this repository.

from __future__ import annotations
from typing import Any
from ... import tl as _tl
from .._tl_schema import _SCHEMA


class ReadHistory:
    def __init__(
        self,
        channel: Any,
        max_id: int,
    ) -> None:
        self.channel = channel
        self.max_id = max_id

    def to_dict(self) -> dict:
        return {"_": "channels.readHistory", **{
            "channel": _tl._resolve(self.channel),
            "max_id": _tl._resolve(self.max_id),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"ReadHistory(channel={self.channel!r}, max_id={self.max_id!r})"

class DeleteMessages:
    def __init__(
        self,
        channel: Any,
        id: list[int],
    ) -> None:
        self.channel = channel
        self.id = id

    def to_dict(self) -> dict:
        return {"_": "channels.deleteMessages", **{
            "channel": _tl._resolve(self.channel),
            "id": _tl._resolve(self.id),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"DeleteMessages(channel={self.channel!r}, id={self.id!r})"

class ReportSpam:
    def __init__(
        self,
        channel: Any,
        participant: Any,
        id: list[int],
    ) -> None:
        self.channel = channel
        self.participant = participant
        self.id = id

    def to_dict(self) -> dict:
        return {"_": "channels.reportSpam", **{
            "channel": _tl._resolve(self.channel),
            "participant": _tl._resolve(self.participant),
            "id": _tl._resolve(self.id),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"ReportSpam(channel={self.channel!r}, participant={self.participant!r}, id={self.id!r})"

class GetMessages:
    def __init__(
        self,
        channel: Any,
        id: list[Any],
    ) -> None:
        self.channel = channel
        self.id = id

    def to_dict(self) -> dict:
        return {"_": "channels.getMessages", **{
            "channel": _tl._resolve(self.channel),
            "id": _tl._resolve(self.id),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"GetMessages(channel={self.channel!r}, id={self.id!r})"

class GetParticipants:
    def __init__(
        self,
        channel: Any,
        filter: Any,
        offset: int,
        limit: int,
        hash: int,
    ) -> None:
        self.channel = channel
        self.filter = filter
        self.offset = offset
        self.limit = limit
        self.hash = hash

    def to_dict(self) -> dict:
        return {"_": "channels.getParticipants", **{
            "channel": _tl._resolve(self.channel),
            "filter": _tl._resolve(self.filter),
            "offset": _tl._resolve(self.offset),
            "limit": _tl._resolve(self.limit),
            "hash": _tl._resolve(self.hash),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"GetParticipants(channel={self.channel!r}, filter={self.filter!r}, offset={self.offset!r}, limit={self.limit!r})"

class GetParticipant:
    def __init__(
        self,
        channel: Any,
        participant: Any,
    ) -> None:
        self.channel = channel
        self.participant = participant

    def to_dict(self) -> dict:
        return {"_": "channels.getParticipant", **{
            "channel": _tl._resolve(self.channel),
            "participant": _tl._resolve(self.participant),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"GetParticipant(channel={self.channel!r}, participant={self.participant!r})"

class GetChannels:
    def __init__(
        self,
        id: list[Any],
    ) -> None:
        self.id = id

    def to_dict(self) -> dict:
        return {"_": "channels.getChannels", **{
            "id": _tl._resolve(self.id),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"GetChannels(id={self.id!r})"

class GetFullChannel:
    def __init__(
        self,
        channel: Any,
    ) -> None:
        self.channel = channel

    def to_dict(self) -> dict:
        return {"_": "channels.getFullChannel", **{
            "channel": _tl._resolve(self.channel),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"GetFullChannel(channel={self.channel!r})"

class CreateChannel:
    def __init__(
        self,
        title: str,
        about: str,
        broadcast: bool | None = None,
        megagroup: bool | None = None,
        for_import: bool | None = None,
        forum: bool | None = None,
        geo_point: Any | None = None,
        address: str | None = None,
        ttl_period: int | None = None,
    ) -> None:
        self.broadcast = broadcast
        self.megagroup = megagroup
        self.for_import = for_import
        self.forum = forum
        self.title = title
        self.about = about
        self.geo_point = geo_point
        self.address = address
        self.ttl_period = ttl_period

    def to_dict(self) -> dict:
        return {"_": "channels.createChannel", **{
            "broadcast": _tl._resolve(self.broadcast),
            "megagroup": _tl._resolve(self.megagroup),
            "for_import": _tl._resolve(self.for_import),
            "forum": _tl._resolve(self.forum),
            "title": _tl._resolve(self.title),
            "about": _tl._resolve(self.about),
            "geo_point": _tl._resolve(self.geo_point),
            "address": _tl._resolve(self.address),
            "ttl_period": _tl._resolve(self.ttl_period),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"CreateChannel(broadcast={self.broadcast!r}, megagroup={self.megagroup!r}, for_import={self.for_import!r}, forum={self.forum!r})"

class EditAdmin:
    def __init__(
        self,
        channel: Any,
        user_id: Any,
        admin_rights: Any,
        rank: str | None = None,
    ) -> None:
        self.channel = channel
        self.user_id = user_id
        self.admin_rights = admin_rights
        self.rank = rank

    def to_dict(self) -> dict:
        return {"_": "channels.editAdmin", **{
            "channel": _tl._resolve(self.channel),
            "user_id": _tl._resolve(self.user_id),
            "admin_rights": _tl._resolve(self.admin_rights),
            "rank": _tl._resolve(self.rank),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"EditAdmin(channel={self.channel!r}, user_id={self.user_id!r}, admin_rights={self.admin_rights!r}, rank={self.rank!r})"

class EditTitle:
    def __init__(
        self,
        channel: Any,
        title: str,
    ) -> None:
        self.channel = channel
        self.title = title

    def to_dict(self) -> dict:
        return {"_": "channels.editTitle", **{
            "channel": _tl._resolve(self.channel),
            "title": _tl._resolve(self.title),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"EditTitle(channel={self.channel!r}, title={self.title!r})"

class EditPhoto:
    def __init__(
        self,
        channel: Any,
        photo: Any,
    ) -> None:
        self.channel = channel
        self.photo = photo

    def to_dict(self) -> dict:
        return {"_": "channels.editPhoto", **{
            "channel": _tl._resolve(self.channel),
            "photo": _tl._resolve(self.photo),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"EditPhoto(channel={self.channel!r}, photo={self.photo!r})"

class CheckUsername:
    def __init__(
        self,
        channel: Any,
        username: str,
    ) -> None:
        self.channel = channel
        self.username = username

    def to_dict(self) -> dict:
        return {"_": "channels.checkUsername", **{
            "channel": _tl._resolve(self.channel),
            "username": _tl._resolve(self.username),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"CheckUsername(channel={self.channel!r}, username={self.username!r})"

class UpdateUsername:
    def __init__(
        self,
        channel: Any,
        username: str,
    ) -> None:
        self.channel = channel
        self.username = username

    def to_dict(self) -> dict:
        return {"_": "channels.updateUsername", **{
            "channel": _tl._resolve(self.channel),
            "username": _tl._resolve(self.username),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"UpdateUsername(channel={self.channel!r}, username={self.username!r})"

class JoinChannel:
    def __init__(
        self,
        channel: Any,
    ) -> None:
        self.channel = channel

    def to_dict(self) -> dict:
        return {"_": "channels.joinChannel", **{
            "channel": _tl._resolve(self.channel),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"JoinChannel(channel={self.channel!r})"

class LeaveChannel:
    def __init__(
        self,
        channel: Any,
    ) -> None:
        self.channel = channel

    def to_dict(self) -> dict:
        return {"_": "channels.leaveChannel", **{
            "channel": _tl._resolve(self.channel),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"LeaveChannel(channel={self.channel!r})"

class InviteToChannel:
    def __init__(
        self,
        channel: Any,
        users: list[Any],
    ) -> None:
        self.channel = channel
        self.users = users

    def to_dict(self) -> dict:
        return {"_": "channels.inviteToChannel", **{
            "channel": _tl._resolve(self.channel),
            "users": _tl._resolve(self.users),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"InviteToChannel(channel={self.channel!r}, users={self.users!r})"

class DeleteChannel:
    def __init__(
        self,
        channel: Any,
    ) -> None:
        self.channel = channel

    def to_dict(self) -> dict:
        return {"_": "channels.deleteChannel", **{
            "channel": _tl._resolve(self.channel),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"DeleteChannel(channel={self.channel!r})"

class ExportMessageLink:
    def __init__(
        self,
        channel: Any,
        id: int,
        grouped: bool | None = None,
        thread: bool | None = None,
    ) -> None:
        self.grouped = grouped
        self.thread = thread
        self.channel = channel
        self.id = id

    def to_dict(self) -> dict:
        return {"_": "channels.exportMessageLink", **{
            "grouped": _tl._resolve(self.grouped),
            "thread": _tl._resolve(self.thread),
            "channel": _tl._resolve(self.channel),
            "id": _tl._resolve(self.id),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"ExportMessageLink(grouped={self.grouped!r}, thread={self.thread!r}, channel={self.channel!r}, id={self.id!r})"

class ToggleSignatures:
    def __init__(
        self,
        channel: Any,
        signatures_enabled: bool | None = None,
        profiles_enabled: bool | None = None,
    ) -> None:
        self.signatures_enabled = signatures_enabled
        self.profiles_enabled = profiles_enabled
        self.channel = channel

    def to_dict(self) -> dict:
        return {"_": "channels.toggleSignatures", **{
            "signatures_enabled": _tl._resolve(self.signatures_enabled),
            "profiles_enabled": _tl._resolve(self.profiles_enabled),
            "channel": _tl._resolve(self.channel),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"ToggleSignatures(signatures_enabled={self.signatures_enabled!r}, profiles_enabled={self.profiles_enabled!r}, channel={self.channel!r})"

class GetAdminedPublicChannels:
    def __init__(
        self,
        by_location: bool | None = None,
        check_limit: bool | None = None,
        for_personal: bool | None = None,
    ) -> None:
        self.by_location = by_location
        self.check_limit = check_limit
        self.for_personal = for_personal

    def to_dict(self) -> dict:
        return {"_": "channels.getAdminedPublicChannels", **{
            "by_location": _tl._resolve(self.by_location),
            "check_limit": _tl._resolve(self.check_limit),
            "for_personal": _tl._resolve(self.for_personal),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"GetAdminedPublicChannels(by_location={self.by_location!r}, check_limit={self.check_limit!r}, for_personal={self.for_personal!r})"

class EditBanned:
    def __init__(
        self,
        channel: Any,
        participant: Any,
        banned_rights: Any,
    ) -> None:
        self.channel = channel
        self.participant = participant
        self.banned_rights = banned_rights

    def to_dict(self) -> dict:
        return {"_": "channels.editBanned", **{
            "channel": _tl._resolve(self.channel),
            "participant": _tl._resolve(self.participant),
            "banned_rights": _tl._resolve(self.banned_rights),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"EditBanned(channel={self.channel!r}, participant={self.participant!r}, banned_rights={self.banned_rights!r})"

class GetAdminLog:
    def __init__(
        self,
        channel: Any,
        q: str,
        max_id: int,
        min_id: int,
        limit: int,
        events_filter: Any | None = None,
        admins: list[Any] | None = None,
    ) -> None:
        self.channel = channel
        self.q = q
        self.events_filter = events_filter
        self.admins = admins
        self.max_id = max_id
        self.min_id = min_id
        self.limit = limit

    def to_dict(self) -> dict:
        return {"_": "channels.getAdminLog", **{
            "channel": _tl._resolve(self.channel),
            "q": _tl._resolve(self.q),
            "events_filter": _tl._resolve(self.events_filter),
            "admins": _tl._resolve(self.admins),
            "max_id": _tl._resolve(self.max_id),
            "min_id": _tl._resolve(self.min_id),
            "limit": _tl._resolve(self.limit),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"GetAdminLog(channel={self.channel!r}, q={self.q!r}, events_filter={self.events_filter!r}, admins={self.admins!r})"

class SetStickers:
    def __init__(
        self,
        channel: Any,
        stickerset: Any,
    ) -> None:
        self.channel = channel
        self.stickerset = stickerset

    def to_dict(self) -> dict:
        return {"_": "channels.setStickers", **{
            "channel": _tl._resolve(self.channel),
            "stickerset": _tl._resolve(self.stickerset),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"SetStickers(channel={self.channel!r}, stickerset={self.stickerset!r})"

class ReadMessageContents:
    def __init__(
        self,
        channel: Any,
        id: list[int],
    ) -> None:
        self.channel = channel
        self.id = id

    def to_dict(self) -> dict:
        return {"_": "channels.readMessageContents", **{
            "channel": _tl._resolve(self.channel),
            "id": _tl._resolve(self.id),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"ReadMessageContents(channel={self.channel!r}, id={self.id!r})"

class DeleteHistory:
    def __init__(
        self,
        channel: Any,
        max_id: int,
        for_everyone: bool | None = None,
    ) -> None:
        self.for_everyone = for_everyone
        self.channel = channel
        self.max_id = max_id

    def to_dict(self) -> dict:
        return {"_": "channels.deleteHistory", **{
            "for_everyone": _tl._resolve(self.for_everyone),
            "channel": _tl._resolve(self.channel),
            "max_id": _tl._resolve(self.max_id),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"DeleteHistory(for_everyone={self.for_everyone!r}, channel={self.channel!r}, max_id={self.max_id!r})"

class TogglePreHistoryHidden:
    def __init__(
        self,
        channel: Any,
        enabled: bool,
    ) -> None:
        self.channel = channel
        self.enabled = enabled

    def to_dict(self) -> dict:
        return {"_": "channels.togglePreHistoryHidden", **{
            "channel": _tl._resolve(self.channel),
            "enabled": _tl._resolve(self.enabled),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"TogglePreHistoryHidden(channel={self.channel!r}, enabled={self.enabled!r})"

class GetLeftChannels:
    def __init__(
        self,
        offset: int,
    ) -> None:
        self.offset = offset

    def to_dict(self) -> dict:
        return {"_": "channels.getLeftChannels", **{
            "offset": _tl._resolve(self.offset),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"GetLeftChannels(offset={self.offset!r})"

class GetGroupsForDiscussion:
    def __init__(self) -> None:
        pass

    def to_dict(self) -> dict:
        return {"_": "channels.getGroupsForDiscussion", **{
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"GetGroupsForDiscussion()"

class SetDiscussionGroup:
    def __init__(
        self,
        broadcast: Any,
        group: Any,
    ) -> None:
        self.broadcast = broadcast
        self.group = group

    def to_dict(self) -> dict:
        return {"_": "channels.setDiscussionGroup", **{
            "broadcast": _tl._resolve(self.broadcast),
            "group": _tl._resolve(self.group),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"SetDiscussionGroup(broadcast={self.broadcast!r}, group={self.group!r})"

class EditLocation:
    def __init__(
        self,
        channel: Any,
        geo_point: Any,
        address: str,
    ) -> None:
        self.channel = channel
        self.geo_point = geo_point
        self.address = address

    def to_dict(self) -> dict:
        return {"_": "channels.editLocation", **{
            "channel": _tl._resolve(self.channel),
            "geo_point": _tl._resolve(self.geo_point),
            "address": _tl._resolve(self.address),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"EditLocation(channel={self.channel!r}, geo_point={self.geo_point!r}, address={self.address!r})"

class ToggleSlowMode:
    def __init__(
        self,
        channel: Any,
        seconds: int,
    ) -> None:
        self.channel = channel
        self.seconds = seconds

    def to_dict(self) -> dict:
        return {"_": "channels.toggleSlowMode", **{
            "channel": _tl._resolve(self.channel),
            "seconds": _tl._resolve(self.seconds),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"ToggleSlowMode(channel={self.channel!r}, seconds={self.seconds!r})"

class GetInactiveChannels:
    def __init__(self) -> None:
        pass

    def to_dict(self) -> dict:
        return {"_": "channels.getInactiveChannels", **{
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"GetInactiveChannels()"

class ConvertToGigagroup:
    def __init__(
        self,
        channel: Any,
    ) -> None:
        self.channel = channel

    def to_dict(self) -> dict:
        return {"_": "channels.convertToGigagroup", **{
            "channel": _tl._resolve(self.channel),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"ConvertToGigagroup(channel={self.channel!r})"

class GetSendAs:
    def __init__(
        self,
        peer: Any,
        for_paid_reactions: bool | None = None,
        for_live_stories: bool | None = None,
    ) -> None:
        self.for_paid_reactions = for_paid_reactions
        self.for_live_stories = for_live_stories
        self.peer = peer

    def to_dict(self) -> dict:
        return {"_": "channels.getSendAs", **{
            "for_paid_reactions": _tl._resolve(self.for_paid_reactions),
            "for_live_stories": _tl._resolve(self.for_live_stories),
            "peer": _tl._resolve(self.peer),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"GetSendAs(for_paid_reactions={self.for_paid_reactions!r}, for_live_stories={self.for_live_stories!r}, peer={self.peer!r})"

class DeleteParticipantHistory:
    def __init__(
        self,
        channel: Any,
        participant: Any,
    ) -> None:
        self.channel = channel
        self.participant = participant

    def to_dict(self) -> dict:
        return {"_": "channels.deleteParticipantHistory", **{
            "channel": _tl._resolve(self.channel),
            "participant": _tl._resolve(self.participant),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"DeleteParticipantHistory(channel={self.channel!r}, participant={self.participant!r})"

class ToggleJoinToSend:
    def __init__(
        self,
        channel: Any,
        enabled: bool,
    ) -> None:
        self.channel = channel
        self.enabled = enabled

    def to_dict(self) -> dict:
        return {"_": "channels.toggleJoinToSend", **{
            "channel": _tl._resolve(self.channel),
            "enabled": _tl._resolve(self.enabled),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"ToggleJoinToSend(channel={self.channel!r}, enabled={self.enabled!r})"

class ToggleJoinRequest:
    def __init__(
        self,
        channel: Any,
        enabled: bool,
        apply_to_invites: bool | None = None,
        guard_bot: Any | None = None,
    ) -> None:
        self.apply_to_invites = apply_to_invites
        self.channel = channel
        self.enabled = enabled
        self.guard_bot = guard_bot

    def to_dict(self) -> dict:
        return {"_": "channels.toggleJoinRequest", **{
            "apply_to_invites": _tl._resolve(self.apply_to_invites),
            "channel": _tl._resolve(self.channel),
            "enabled": _tl._resolve(self.enabled),
            "guard_bot": _tl._resolve(self.guard_bot),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"ToggleJoinRequest(apply_to_invites={self.apply_to_invites!r}, channel={self.channel!r}, enabled={self.enabled!r}, guard_bot={self.guard_bot!r})"

class ReorderUsernames:
    def __init__(
        self,
        channel: Any,
        order: list[str],
    ) -> None:
        self.channel = channel
        self.order = order

    def to_dict(self) -> dict:
        return {"_": "channels.reorderUsernames", **{
            "channel": _tl._resolve(self.channel),
            "order": _tl._resolve(self.order),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"ReorderUsernames(channel={self.channel!r}, order={self.order!r})"

class ToggleUsername:
    def __init__(
        self,
        channel: Any,
        username: str,
        active: bool,
    ) -> None:
        self.channel = channel
        self.username = username
        self.active = active

    def to_dict(self) -> dict:
        return {"_": "channels.toggleUsername", **{
            "channel": _tl._resolve(self.channel),
            "username": _tl._resolve(self.username),
            "active": _tl._resolve(self.active),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"ToggleUsername(channel={self.channel!r}, username={self.username!r}, active={self.active!r})"

class DeactivateAllUsernames:
    def __init__(
        self,
        channel: Any,
    ) -> None:
        self.channel = channel

    def to_dict(self) -> dict:
        return {"_": "channels.deactivateAllUsernames", **{
            "channel": _tl._resolve(self.channel),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"DeactivateAllUsernames(channel={self.channel!r})"

class ToggleForum:
    def __init__(
        self,
        channel: Any,
        enabled: bool,
        tabs: bool,
    ) -> None:
        self.channel = channel
        self.enabled = enabled
        self.tabs = tabs

    def to_dict(self) -> dict:
        return {"_": "channels.toggleForum", **{
            "channel": _tl._resolve(self.channel),
            "enabled": _tl._resolve(self.enabled),
            "tabs": _tl._resolve(self.tabs),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"ToggleForum(channel={self.channel!r}, enabled={self.enabled!r}, tabs={self.tabs!r})"

class ToggleAntiSpam:
    def __init__(
        self,
        channel: Any,
        enabled: bool,
    ) -> None:
        self.channel = channel
        self.enabled = enabled

    def to_dict(self) -> dict:
        return {"_": "channels.toggleAntiSpam", **{
            "channel": _tl._resolve(self.channel),
            "enabled": _tl._resolve(self.enabled),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"ToggleAntiSpam(channel={self.channel!r}, enabled={self.enabled!r})"

class ReportAntiSpamFalsePositive:
    def __init__(
        self,
        channel: Any,
        msg_id: int,
    ) -> None:
        self.channel = channel
        self.msg_id = msg_id

    def to_dict(self) -> dict:
        return {"_": "channels.reportAntiSpamFalsePositive", **{
            "channel": _tl._resolve(self.channel),
            "msg_id": _tl._resolve(self.msg_id),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"ReportAntiSpamFalsePositive(channel={self.channel!r}, msg_id={self.msg_id!r})"

class ToggleParticipantsHidden:
    def __init__(
        self,
        channel: Any,
        enabled: bool,
    ) -> None:
        self.channel = channel
        self.enabled = enabled

    def to_dict(self) -> dict:
        return {"_": "channels.toggleParticipantsHidden", **{
            "channel": _tl._resolve(self.channel),
            "enabled": _tl._resolve(self.enabled),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"ToggleParticipantsHidden(channel={self.channel!r}, enabled={self.enabled!r})"

class UpdateColor:
    def __init__(
        self,
        channel: Any,
        for_profile: bool | None = None,
        color: int | None = None,
        background_emoji_id: int | None = None,
    ) -> None:
        self.for_profile = for_profile
        self.channel = channel
        self.color = color
        self.background_emoji_id = background_emoji_id

    def to_dict(self) -> dict:
        return {"_": "channels.updateColor", **{
            "for_profile": _tl._resolve(self.for_profile),
            "channel": _tl._resolve(self.channel),
            "color": _tl._resolve(self.color),
            "background_emoji_id": _tl._resolve(self.background_emoji_id),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"UpdateColor(for_profile={self.for_profile!r}, channel={self.channel!r}, color={self.color!r}, background_emoji_id={self.background_emoji_id!r})"

class ToggleViewForumAsMessages:
    def __init__(
        self,
        channel: Any,
        enabled: bool,
    ) -> None:
        self.channel = channel
        self.enabled = enabled

    def to_dict(self) -> dict:
        return {"_": "channels.toggleViewForumAsMessages", **{
            "channel": _tl._resolve(self.channel),
            "enabled": _tl._resolve(self.enabled),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"ToggleViewForumAsMessages(channel={self.channel!r}, enabled={self.enabled!r})"

class GetChannelRecommendations:
    def __init__(
        self,
        channel: Any | None = None,
    ) -> None:
        self.channel = channel

    def to_dict(self) -> dict:
        return {"_": "channels.getChannelRecommendations", **{
            "channel": _tl._resolve(self.channel),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"GetChannelRecommendations(channel={self.channel!r})"

class UpdateEmojiStatus:
    def __init__(
        self,
        channel: Any,
        emoji_status: Any,
    ) -> None:
        self.channel = channel
        self.emoji_status = emoji_status

    def to_dict(self) -> dict:
        return {"_": "channels.updateEmojiStatus", **{
            "channel": _tl._resolve(self.channel),
            "emoji_status": _tl._resolve(self.emoji_status),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"UpdateEmojiStatus(channel={self.channel!r}, emoji_status={self.emoji_status!r})"

class SetBoostsToUnblockRestrictions:
    def __init__(
        self,
        channel: Any,
        boosts: int,
    ) -> None:
        self.channel = channel
        self.boosts = boosts

    def to_dict(self) -> dict:
        return {"_": "channels.setBoostsToUnblockRestrictions", **{
            "channel": _tl._resolve(self.channel),
            "boosts": _tl._resolve(self.boosts),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"SetBoostsToUnblockRestrictions(channel={self.channel!r}, boosts={self.boosts!r})"

class SetEmojiStickers:
    def __init__(
        self,
        channel: Any,
        stickerset: Any,
    ) -> None:
        self.channel = channel
        self.stickerset = stickerset

    def to_dict(self) -> dict:
        return {"_": "channels.setEmojiStickers", **{
            "channel": _tl._resolve(self.channel),
            "stickerset": _tl._resolve(self.stickerset),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"SetEmojiStickers(channel={self.channel!r}, stickerset={self.stickerset!r})"

class RestrictSponsoredMessages:
    def __init__(
        self,
        channel: Any,
        restricted: bool,
    ) -> None:
        self.channel = channel
        self.restricted = restricted

    def to_dict(self) -> dict:
        return {"_": "channels.restrictSponsoredMessages", **{
            "channel": _tl._resolve(self.channel),
            "restricted": _tl._resolve(self.restricted),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"RestrictSponsoredMessages(channel={self.channel!r}, restricted={self.restricted!r})"

class SearchPosts:
    def __init__(
        self,
        offset_rate: int,
        offset_peer: Any,
        offset_id: int,
        limit: int,
        hashtag: str | None = None,
        query: str | None = None,
        allow_paid_stars: int | None = None,
    ) -> None:
        self.hashtag = hashtag
        self.query = query
        self.offset_rate = offset_rate
        self.offset_peer = offset_peer
        self.offset_id = offset_id
        self.limit = limit
        self.allow_paid_stars = allow_paid_stars

    def to_dict(self) -> dict:
        return {"_": "channels.searchPosts", **{
            "hashtag": _tl._resolve(self.hashtag),
            "query": _tl._resolve(self.query),
            "offset_rate": _tl._resolve(self.offset_rate),
            "offset_peer": _tl._resolve(self.offset_peer),
            "offset_id": _tl._resolve(self.offset_id),
            "limit": _tl._resolve(self.limit),
            "allow_paid_stars": _tl._resolve(self.allow_paid_stars),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"SearchPosts(hashtag={self.hashtag!r}, query={self.query!r}, offset_rate={self.offset_rate!r}, offset_peer={self.offset_peer!r})"

class UpdatePaidMessagesPrice:
    def __init__(
        self,
        channel: Any,
        send_paid_messages_stars: int,
        broadcast_messages_allowed: bool | None = None,
    ) -> None:
        self.broadcast_messages_allowed = broadcast_messages_allowed
        self.channel = channel
        self.send_paid_messages_stars = send_paid_messages_stars

    def to_dict(self) -> dict:
        return {"_": "channels.updatePaidMessagesPrice", **{
            "broadcast_messages_allowed": _tl._resolve(self.broadcast_messages_allowed),
            "channel": _tl._resolve(self.channel),
            "send_paid_messages_stars": _tl._resolve(self.send_paid_messages_stars),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"UpdatePaidMessagesPrice(broadcast_messages_allowed={self.broadcast_messages_allowed!r}, channel={self.channel!r}, send_paid_messages_stars={self.send_paid_messages_stars!r})"

class ToggleAutotranslation:
    def __init__(
        self,
        channel: Any,
        enabled: bool,
    ) -> None:
        self.channel = channel
        self.enabled = enabled

    def to_dict(self) -> dict:
        return {"_": "channels.toggleAutotranslation", **{
            "channel": _tl._resolve(self.channel),
            "enabled": _tl._resolve(self.enabled),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"ToggleAutotranslation(channel={self.channel!r}, enabled={self.enabled!r})"

class GetMessageAuthor:
    def __init__(
        self,
        channel: Any,
        id: int,
    ) -> None:
        self.channel = channel
        self.id = id

    def to_dict(self) -> dict:
        return {"_": "channels.getMessageAuthor", **{
            "channel": _tl._resolve(self.channel),
            "id": _tl._resolve(self.id),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"GetMessageAuthor(channel={self.channel!r}, id={self.id!r})"

class CheckSearchPostsFlood:
    def __init__(
        self,
        query: str | None = None,
    ) -> None:
        self.query = query

    def to_dict(self) -> dict:
        return {"_": "channels.checkSearchPostsFlood", **{
            "query": _tl._resolve(self.query),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"CheckSearchPostsFlood(query={self.query!r})"

class SetMainProfileTab:
    def __init__(
        self,
        channel: Any,
        tab: Any,
    ) -> None:
        self.channel = channel
        self.tab = tab

    def to_dict(self) -> dict:
        return {"_": "channels.setMainProfileTab", **{
            "channel": _tl._resolve(self.channel),
            "tab": _tl._resolve(self.tab),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"SetMainProfileTab(channel={self.channel!r}, tab={self.tab!r})"
