# Copyright (c) Ankit Chaubey <ankitchaubey.dev@gmail.com>
# SPDX-License-Identifier: MIT OR Apache-2.0
#
# ferogram is a high-performance Telegram MTProto framework written in Rust.
# ferogram-py provides Python bindings built on top of the Rust core for
# building Telegram clients, bots, and applications with a simple API.
#
# Rust core: https://github.com/ankit-chaubey/ferogram
# Python bindings: https://github.com/ankit-chaubey/ferogram-py
#
# If you use or modify this code, keep this notice at the top of the file
# and include the LICENSE-MIT or LICENSE-APACHE file from this repository.


# auto-generated by ferogram raw codegen - do not edit
from __future__ import annotations
from typing import Any
from ... import tl as _tl
from .._tl_schema import _SCHEMA


class GetBoostsList:
    def __init__(
        self,
        peer: Any,
        offset: str,
        limit: int,
        gifts: bool | None = None,
    ) -> None:
        self.gifts = gifts
        self.peer = peer
        self.offset = offset
        self.limit = limit

    def to_dict(self) -> dict:
        return {"_": "premium.getBoostsList", **{
            "gifts": _tl._resolve(self.gifts),
            "peer": _tl._resolve(self.peer),
            "offset": _tl._resolve(self.offset),
            "limit": _tl._resolve(self.limit),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"GetBoostsList(gifts={self.gifts!r}, peer={self.peer!r}, offset={self.offset!r}, limit={self.limit!r})"

class GetMyBoosts:
    def __init__(self) -> None:
        pass

    def to_dict(self) -> dict:
        return {"_": "premium.getMyBoosts", **{
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"GetMyBoosts()"

class ApplyBoost:
    def __init__(
        self,
        peer: Any,
        slots: list[int] | None = None,
    ) -> None:
        self.slots = slots
        self.peer = peer

    def to_dict(self) -> dict:
        return {"_": "premium.applyBoost", **{
            "slots": _tl._resolve(self.slots),
            "peer": _tl._resolve(self.peer),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"ApplyBoost(slots={self.slots!r}, peer={self.peer!r})"

class GetBoostsStatus:
    def __init__(
        self,
        peer: Any,
    ) -> None:
        self.peer = peer

    def to_dict(self) -> dict:
        return {"_": "premium.getBoostsStatus", **{
            "peer": _tl._resolve(self.peer),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"GetBoostsStatus(peer={self.peer!r})"

class GetUserBoosts:
    def __init__(
        self,
        peer: Any,
        user_id: Any,
    ) -> None:
        self.peer = peer
        self.user_id = user_id

    def to_dict(self) -> dict:
        return {"_": "premium.getUserBoosts", **{
            "peer": _tl._resolve(self.peer),
            "user_id": _tl._resolve(self.user_id),
        }}

    def to_bytes(self) -> bytes:
        return _tl.serialize_object(self.to_dict(), _SCHEMA)

    def __repr__(self) -> str:
        return f"GetUserBoosts(peer={self.peer!r}, user_id={self.user_id!r})"
